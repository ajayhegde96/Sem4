16) select name,title,T.year from takes T,section S,student1 ST, course C where ST.ID=T.ID and C.course_ID=T.course_ID and T.course_ID=S.course_ID and T.sec_ID=S.sec_ID and T.semester = S.semester and T.year = S.year and room_number=101;
20)select T.course_ID,T1.title,T2.title from prereq T,course T1,course T2 where T.course_ID=T1.course_ID and T.prereq_ID=T2.course_ID order by T.course_ID,T.prereq_ID;



21) select title from section,course where course.course_ID=section.course_ID and semester='Fall' and year = 2009 union select title from section,course where course.course_ID=section.course_ID and semester='Spring' and year = 2010;

22)select title from section,course where course.course_ID=section.course_ID and semester='Fall' and year = 2009 intersect select title from section,course where course.course_ID=section.course_ID and semester='Spring' and year = 2010;

23)select title from section,course where course.course_ID=section.course_ID and semester='Fall' and year = 2009 minus select title from section,course where course.course_ID=section.course_ID and semester='Spring' and year = 2010;

24) select avg(salary) from instructor where dept_name='Comp. Sci.';

25) select count(distinct ID) from teaches where semester = 'Spring' and year = 2010;

26) select course_ID,count(ID) from takes group by course_ID;

27) select dept_name,count(ID) from student1 group by dept_name having count(ID)>2;

28) select dept_name,count(course_ID) from course group by dept_name;

29) select dept_name,avg(salary) from instructor group by dept_name having avg(Salary)>42000;

30) select course_ID from course where course_ID not in (select distinct course_ID from takes);

31) select course_ID from section where semester='Fall' and year =2009 and course_ID in (select course_ID from section where semester='Spring' and year = 2010);

32) select count(distinct ID) from takes where (course_ID,sec_ID,semester,year) in (select course_ID,sec_ID,semester,year from teaches where teaches.ID=10101);

33) select course_ID from section where semester='Fall' and year =2009 and course_ID not in (select course_ID from section where semester='Spring' and year = 2010);

34) select name,salary from instructor where salary >some (select salary from instructor where dept_name = 'Biology');
35) select name,salary from instructor where salary >all (select salary from instructor where dept_name = 'Biology');

36) select course_ID from section S where semester='Fall' and year =2009 and exists (select course_ID from section T where semester ='Spring' and year = 2010 and S.course_ID=T.course_ID);

37) select distinct S.ID ,S.name from student1 S where not exists (( select course_ID from course where dept_name = 'Physics') minus (select T.course_ID from takes T where S.ID=T.ID));

40) select max(tot_salary) from (select dept_name, sum(salary) as tot_salary from instructor group by dept_name); 

41) select avg(avg_salary) from (select dept_name, avg(salary) as avg_salary from instructor group by dept_name) where avg_Salary > 42000;

42) with max_budget(value) as (select max(budget) from department) select dept_name,budget from department,max_budget where  department.budget=max_budget.value; 

43) with dept_tot(dept_name,value) as (select dept_name, sum(salary) from instructor group by dept_name), 
dept_totav(value1) as (select avg(value) from dept_tot) 
select dept_name from dept_tot,dept_totav where dept_tot.value >= dept_totav.value1; 

44) create view all_Courses as select course.course_ID,title,sec_ID,building,room_number from course,section where course.course_ID=section.course_ID and course.dept_name='Physics' and section.semester = 'Fall' and section.year=2009;

45) select * from all_Course;